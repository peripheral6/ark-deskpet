# 发布检查

当前版本是 Windows Python 源码项目，不是免安装 exe，也不是 Codex 原生 Custom pets。

## 已实现功能

- 全部本地任务或指定任务的只读、增量事件监听。
- 任一任务运行时保持移动，全部结束后显示最后的结束状态。
- 中英文状态字幕、透明居中、完成字幕10秒后隐藏。
- 同一安装目录防重复启动；启动、显示和动作尺寸变化时恢复屏幕内位置。
- 遥与予愿安洁莉娜角色库。

## 发布前复核

- 在安装 PySide6 的 Windows Python 中运行 `python -m unittest discover -s tests -v`。
- 新目录首次安装、实际桌面显示、两个任务并发及完成/中断的人工验收。
- 不提交 settings.json、.venv、日志、PID/flag、凭据或个人任务ID。
- 保留原作者和素材来源说明，不擅自替上游代码或角色素材添加许可证。
- 上游代码许可证及角色素材再分发授权：仍待确认，公开仓库不表示此项已完成。

## 已知限制

- 默认汇总所有 sessions 历史日志；首次启动需追赶历史数据。
- 若崩溃后未写入终止事件，旧任务可能持续显示运行中；重启桌宠并不会补出缺失事件。
- 不读取云端或没有本地 rollout 文件的任务，也不等于监听当前前台页面。
- 实际延迟还包括日志落盘时间；新文件发现周期约1秒。
- 防重复启动按安装目录隔离，不同目录可分别运行。

角色素材已经包含在版本历史中；仅在最新提交删除素材不会从旧提交中移除。此检查单不代表授权已经取得。
